// 处理git钩子
husky
// v7版本
// npx husky-init
// npm install
// npx husky add .husky/commit-msg
// 填充commit文件中的undefined为：npx --no-install commitlint --edit "$1"

// 格式化代码风格
lint-staged
// npm install lint-staged --save-dev

// 设置环境变量（windows不支持NODE_ENV=development的设置方式。）
cross-env


// prettier
// npm install --save-dev --save-exact prettier
